# notebooks/ — 탐색 분석

확정된 파이프라인은 `src/`에, **탐색 단계의 분석**은 여기에 둡니다.
노트북은 GitHub에서 그대로 렌더링되므로 결과를 바로 볼 수 있습니다.

| 노트북 | 무엇을 보나 | 작성 |
|---|---|---|
| [`01_이변분석.ipynb`](01_이변분석.ipynb) | 이변(upset)이 어떤 조건에서 일어나는지 · 유형 분류 | 전유림 (`yoolim-jeon`) |

여기서 얻은 관찰이 프로젝트 흐름 **5단계**의 방향 전환 근거가 됐고,
정리된 코드는 [`src/4_이변모델/`](../src/4_이변모델)과 [`src/5_이변모델_선정/`](../src/5_이변모델_선정)에 있습니다.

## 실행

`notebooks/` 폴더에서 열면 상대 경로가 맞습니다.

```python
df = pd.read_csv("../data/raw/race_entries.csv.gz", compression="gzip")
```
